# Required for the newsletter signup endpoint (/api/subscribe).
# Create a server API key in Loops: https://app.loops.so/settings?page=api
LOOPS_API_KEY=

# Required for /playbook/admin to authorize comment moderation.
# Set to any random secret string. Use the same value in Netlify env vars.
ADMIN_TOKEN=
